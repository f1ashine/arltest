name: Ubuntu
on:
  workflow_run:
   workflows: ["arltest"]
   branches: [main]
   types: 
      - completed
jobs:
  Ubuntu:
    runs-on: ubuntu-latest
    steps:
    - name: arltest
      uses: h0ppays/arltest@master
